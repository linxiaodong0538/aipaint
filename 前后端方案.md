0

## ✅ 这版 UI 的最终确认（完美版）
1.  **结构完整，逻辑闭环**
    - 参考图上传（最多4张，文生/图生合一）
    - 画面描述（prompt 输入）
    - 模型选择（预留扩展，默认GPT-image-2）
    - 分辨率（1K/2K/4K）
    - 画面比例（1:1/4:3/3:4/16:9/9:16/2:1）
    - 画质选择（低/中/高，默认选中「高」）

2.  **完全适配接口规则**
    - 分辨率与画质档位可自由组合（无硬冲突）
    - 4K 比例过滤逻辑可直接实现（选中4K时，自动隐藏1:1/4:3/3:4）
    - 参考图数量上限与接口兼容（4张≤接口支持的16张）

3.  **用户体验友好**
    - 层级清晰，从上到下完全符合用户创作决策流
    - 每个模块的文案都足够直白，用户一眼就能懂
    - 没有多余的入口和功能，页面清爽不杂乱

---

## 📝 最终版「前后端落地逻辑」
### 一、前端逻辑
#### 1. 初始默认参数
```json
{
  "model": "gpt-image-2",
  "prompt": "",
  "size": "1:1",
  "resolution": "2k",
  "quality": "high",
  "image_urls": []
}
```

#### 2. 核心联动规则
| 操作场景 | 前端行为 | 目的 |
| :--- | :--- | :--- |
| 选中 4K 分辨率 | 自动隐藏 `1:1/4:3/3:4`，仅保留 `16:9/9:16/2:1`；若当前选中比例失效，自动重置为 `9:16` | 避免接口报错 |
| 选中 `1K + 高画质`，点击生成 | 弹出优化引导弹窗（文案和交互按之前定的执行） | 保护用户不踩坑 |
| 提示词为空 | 「开始生成」按钮置灰不可点击 | 避免无效请求 |
| 上传参考图 | 拼接图片公网 URL 数组，传给 `image_urls`；最多4张，超出则提示 | 适配接口多图参考能力 |


---

### 二、后端逻辑
1.  **接口请求参数映射**
    - `model`：前端传什么就用什么（当前默认 `gpt-image-2`）
    - `prompt`：直接透传
    - `size`：前端固定传6个比例之一，无需处理 `auto`
    - `resolution`：直接透传 `1k/2k/4k`
    - `quality`：直接透传 `low/medium/high`
    - `image_urls`：有则透传，无则传空数组

2.  **生成状态回传**
    - 按接口返回的 `status` 和 `progress` 回传给前端
    - 异常失败时返回 `failed` 状态，前端展示通用错误提示
